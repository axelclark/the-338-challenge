<%= form_tag("/fantasy_players", method: "get") do %>
  <p>Change Fantasy League</p>
  <%= select_tag(:fantasy_league_id, 
        options_from_collection_for_select(FantasyLeague.all, :id, :name)) %>
  <%= submit_tag "Update" %>
<% end %>  
